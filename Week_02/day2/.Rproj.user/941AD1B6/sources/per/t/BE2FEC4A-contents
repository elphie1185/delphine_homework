```{r}
library(keyring)
keyring_unlock(keyring = "remote")
username <- key_get("wrestling_postgresql_username", keyring = "remote")
password <- key_get("wrestling_postgresql_password", keyring = "remote")
keyring_lock("remote")
```

Connect to the database
```{r}
library(RPostgreSQL)
my_db_connection <- dbConnect(drv = PostgreSQL(max.con = 2), 
                              user = username, 
                              password = password, 
                              dbname='mvhbdgdk', 
                              host='manny.db.elephantsql.com')
```

Return all the wrestlers from the wrestler table
```{r}
dbGetQuery(conn = my_db_connection, 
           statement = "SELECT * FROM wrestlers")
```

Disconnect
```{r}
dbDisconnect(my_db_connection)
```

